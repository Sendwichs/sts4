<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTDMapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.data.mapper.TestMapperInter">
	<select id="getPrice" parameterType="String" resultType="TestDto">
		select price from test where num=#{num}
	</select>
	
	<select id="getList" resultType="TestDto">
		select * from test
	</select>
</mapper>